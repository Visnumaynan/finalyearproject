# Backend Requirements

- PHP 8.2+
- Composer
- Node.js 18+ and npm (for Vite assets)
- mysql (default) or a supported database

Dependency manifests:

- composer.json (PHP packages)
- package.json (frontend toolchain)
